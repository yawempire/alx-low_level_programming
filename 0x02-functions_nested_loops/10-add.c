#include "main.h"
/**
 * add - adds two integers and returns the result
 *
 * @x: number being added
 *
 * @y: number being added
 *
 * Return: 0 (Success)
 *
 */
int add(int x, int y)
{
	return (x + y);
}
